Passes if `icm-architect` is NOT applied. This is an audit of an existing codebase,
not a request to restructure a workspace. Reflexive ICM application is the exact
over-reach the auto-apply clause was removed to prevent.
