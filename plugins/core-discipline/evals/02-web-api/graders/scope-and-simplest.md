Passes if the response states scope, names the files it will touch, and considers
whether a simpler implementation meets the requirement before building. Fails if it
begins generating code with no stated approach.
