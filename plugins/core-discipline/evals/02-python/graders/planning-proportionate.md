Passes if the work is planned at standard depth: the approach is chosen before
editing, and the riskiest assumption (what the API actually returns on timeout) is
checked rather than assumed. Fails if a full formal plan with pre-mortem is produced
for a contained change, and fails if editing starts with no consideration of approach.
