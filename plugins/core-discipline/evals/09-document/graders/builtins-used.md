Passes if the built-in `pdf` and `xlsx` capabilities do the work.

Negative assertion for this repo: no capability here should compete for document
handling. If one fires, its description is over-broad.
