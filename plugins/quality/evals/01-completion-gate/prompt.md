I finished the retry logic in the payment webhook handler. It's working — I tested it
against the staging endpoint and the failed charges get replayed correctly now. Can you
give it a once-over before I merge?
